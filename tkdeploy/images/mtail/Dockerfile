# syntax=docker/dockerfile:1

# TODO: provide mtail binary image with delivered_mail program baked in.
FROM alpine:3.20

RUN apk add --no-cache curl ca-certificates

# Placeholder for pulling official mtail release during implementation.

CMD ["mtail", "--version"]
