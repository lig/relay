#!/bin/sh
set -euf

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
ROOT_DIR=$(CDPATH= cd -- "${SCRIPT_DIR}/.." && pwd)

if ! command -v tk >/dev/null 2>&1; then
  echo "error: tk (Grafana Tanka) is not available on PATH" >&2
  exit 127
fi

cd "${ROOT_DIR}"

exec tk "$@"
